name: CI
on:
  push:
    branches:
      - master
  workflow_dispatch: null
jobs:
  build:
    steps:
      - name: docsify-action
        uses: tool3/docsify-action@v1.0.3
        with: null
        github_token: ${{ secrets.GP_TOKEN }}
        dir: null
        branch: null
        docsify_args: null
        commit_msg: null
